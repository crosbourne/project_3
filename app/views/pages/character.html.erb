<h1>CHARACTER</h1>


<button class="A-F">A-F,#</button>
<button class="G-F">G-F</button>
<button class="M-R">M-R</button>
<button class="S-Z">S-Z</button>

      <p>

        <% @characters.to_a.each do |character| %>
        <%= character.name %>
        <%#= character.description.try(:html_safe) %><br>
        <% if character.image.present? %>
        <%= link_to image_tag(character.image["small_url"], height: '160', width: '110') @character%><br>
        <% else %>
        <%= image_tag("no_comic_cover.jpg", height: '160', width: '110')  %><br>
        <% end %>
        <% end %>
        </p>
